import { boot } from 'quasar/wrappers'

// Pinia persisted state is configured in src/stores/index.ts
// This boot file is a placeholder to ensure the boot array entry is valid

export default boot(() => {
  // Nothing to do here — pinia plugin is registered in stores/index.ts
})
